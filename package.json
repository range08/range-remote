{
  "name": "range-remote",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "build": "npm run build --workspace @range-remote/shared && npm run build --workspace @range-remote/agent && npm run build --workspace @range-remote/server",
    "check": "npm run build --workspace @range-remote/shared && npm run check --workspace @range-remote/agent && npm run check --workspace @range-remote/server",
    "test": "npm run build --workspace @range-remote/shared && npm run test --workspace @range-remote/agent && npm run test --workspace @range-remote/server",
    "dev:server": "npm --workspace @range-remote/server run dev",
    "dev:agent": "npm --workspace @range-remote/agent run dev"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^6.0.0",
    "vitest": "^5.0.1"
  }
}
