services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    env_file:
      - .env
    volumes:
      - range_remote_data:/app/data
    ports:
      - "127.0.0.1:8787:8787"
    restart: unless-stopped

volumes:
  range_remote_data:
