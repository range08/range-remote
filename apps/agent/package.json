{
  "name": "@range-remote/agent",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "range-remote-agent": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js start",
    "test": "vitest run src"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@range-remote/shared": "0.1.0",
    "smol-toml": "^1.8.0",
    "ws": "^8.18.3",
    "yaml": "^2.9.1",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.6"
  }
}
