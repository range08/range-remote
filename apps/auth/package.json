{
  "name": "@range-remote/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run src"
  },
  "dependencies": {
    "express": "^5.1.0",
    "oidc-provider": "9.12.2",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/oidc-provider": "^9.12.1",
    "tsx": "^4.20.6"
  }
}
